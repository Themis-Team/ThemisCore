#include "ed_rpl_cutoff.h"

namespace VRT2 {

ED_RPL_Cutoff::ED_RPL_Cutoff(double density_scale, double power, double h, double rmin)
  : _density_scale(density_scale), _power(power), _h(h), _rmin(rmin)
{
}

};
